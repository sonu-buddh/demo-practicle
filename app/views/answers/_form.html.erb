<%= form_with(model: answer) do |form| %>
    <%= form.text_area :description, rows: "1", cols: "100" %>

    <%= form.hidden_field :user_id, value: current_user.id %>

    <%= form.hidden_field :question_id, value: question_id %>

    <%= form.submit "Add" %>
<% end %>
